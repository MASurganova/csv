Импортер данных из файла test_case.csv в базу данных PostgreSQL.
Заполнение базы происходит при старте приложения, поэтому старт занимает некоторое время.

По импортированным данным сформированы три типа отчетов.

А) /lastHour - выводит список пользователей и используемых ими форм за последний час, но так как
   данных в файле за последний час нет, то добавила форму для ввода времени поиска, форма заполнена
   значениями времени для поиска за одну минуту из файла, для которой есть данные, но можно вввести
   и свои значения времени для поиска.

Б) /unfinished - выводит список пользователей (уникальный идентификатор пользователя, время, тип
   и подтип события), которые начали активность на форме и не дошли до конца. Поиск идет для каждого
   пользователя (ssoid) - по каждой форме (formid) выбирается последнее по времени состояние и
   проверяется на 'send' или 'success', если отличается, то считается незаконченным и попадает в
   выборку.

В) /topForms - составляет ТОП-5 самых используемых форм, выводит форму и количесвто обращений.